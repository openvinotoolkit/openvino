name: 'Close stale issues and PRs'
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v8
        with:
          stale-issue-message: 'This issue will be closed in 9 months in case of no activity.'
          stale-pr-message: 'This PR will be closed in one week in case of no activity.'
          close-issue-message: 'This issue was closed because it has been stalled for 9 months with no activity.'
          close-pr-message: 'This PR was closed because it has been stalled for one week with no activity.'
          days-before-stale: 14
          days-before-pr-close: 7
          days-before-issue-close: 274
          ascending: true
          operations-per-run: 5000
          repo-token: ${{ secrets.STALE_ISSUES_TOKEN }}
          exempt-pr-labels: 'no_stale'
