# FastAPI OpenVINO Advanced Example

This example extends the basic FastAPI inference example with:

- async request handling
- basic batching support
- logging and inference timing

## Run

uvicorn fastapi_inference_advanced:app --reload