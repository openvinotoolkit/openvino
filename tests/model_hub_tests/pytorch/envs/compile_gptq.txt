# TorchFX GPTQ Pattern test environment overrides
# Requires older torch (2.3.x) — newer is not yet supported by openvino backend
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1
