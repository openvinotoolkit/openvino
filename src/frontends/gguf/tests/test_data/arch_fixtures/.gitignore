# The *.gguf.hdr fixtures are generated by ../../gen_arch_fixtures.py, not committed -- see the
# manifest header and test_arch_conversion.cpp.  Running the generator in-tree is the supported way
# to make the arch suite live locally, so keep its output out of `git status`.
*.gguf.hdr
