ir_version: 8
producer_name: "OpenVINO ONNX Frontend"
# Diamond dependency pattern (unsorted):
#   X -> Relu -> R1     (node index 0 in sorted)
#   X -> Abs  -> R2     (node index 1 in sorted)
#   R1 + R2   -> Y      (node index 2 in sorted)
# Nodes given in order: Add(R1,R2->Y), Abs(X->R2), Relu(X->R1)
# Multiple valid orderings exist: {Relu, Abs, Add} or {Abs, Relu, Add}.
graph {
  name: "diamond_unsorted_graph"
  node {
    input: "R1"
    input: "R2"
    output: "Y"
    op_type: "Add"
  }
  node {
    input: "X"
    output: "R2"
    op_type: "Abs"
  }
  node {
    input: "X"
    output: "R1"
    op_type: "Relu"
  }
  input {
    name: "X"
    type {
      tensor_type {
        elem_type: 1
        shape {
          dim {
            dim_value: 3
          }
        }
      }
    }
  }
  output {
    name: "Y"
    type {
      tensor_type {
        elem_type: 1
        shape {
          dim {
            dim_value: 3
          }
        }
      }
    }
  }
}
opset_import {
  version: 13
}
