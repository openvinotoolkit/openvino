<!--
Example:
    <skip_config>
        <message>skip_message_xxxxxx</message>
        <enable_rules>
            <backend>LEVEL0</backend>
            <backend></backend> (special case for no backend)
            <device>3720</device>
            <device>!4000</device> (using "!" to negate rule)
            <operating_system>windows</operating_system>
            <operating_system>linux</operating_system>
        </enable_rules>
        <filters>
            <filter>skip_filter_xxxxxxxxxx</filter>
            <filter>skip_filter_xxxxxxxxxx</filter>
            <filter>skip_filter_xxxxxxxxxx</filter>
        </filters>
    </skip_config>
-->
<skip_configs>

    <!-- E###### -->
    <skip_config>
        <message>GetExecGraphInfo function is not implemented for NPU plugin</message>
        <filters>
            <filter>.*CheckExecGraphInfo.*</filter>
        </filters>
    </skip_config>

    <!-- E###### -->
    <skip_config>
        <message>TensorIterator layer is not supported</message>
        <filters>
            <filter>.*OVInferenceChaining.*</filter>
        </filters>
    </skip_config>

    <!-- E#97469 -->
    <skip_config>
        <message>Tests with unsupported precision</message>
        <filters>
            <filter>.*InferRequestCheckTensorPrecision.*type=boolean.*device=MULTI.*</filter>
            <filter>.*InferRequestCheckTensorPrecision.*type=boolean.*device=AUTO.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=boolean.*device=MULTI.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=boolean.*device=AUTO.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Data type u1 not supported by NPU</message>
        <filters>
            <filter>.*InferRequestCheckTensorPrecision.*type=u1\D.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=u1\D.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Data type u2 and 4-bit network outputs not supported by NPU3720</message>
        <enable_rules>
            <device>3720</device>
        </enable_rules>
        <filters>
            <filter>.*InferRequestCheckTensorPrecision.*type=u2.*</filter>
            <filter>.*InferRequestCheckTensorPrecision.*type=i4.*</filter>
            <filter>.*InferRequestCheckTensorPrecision.*type=u4.*</filter>
            <filter>.*InferRequestCheckTensorPrecision.*type=nf4.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=u2.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=i4.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=u4.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=nf4.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Data type f4e2m1 not supported by NPU</message>
        <filters>
            <filter>.*InferRequestCheckTensorPrecision.*type=f4e2m1.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=f4e2m1.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Data type f8e8m0 not supported by NPU</message>
        <filters>
            <filter>.*InferRequestCheckTensorPrecision.*type=f8e8m0.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=f8e8m0.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Data types f8e4m3, f8e5m2 not supported by NPU3720, NPU4000</message>
        <enable_rules>
            <device>3720</device>
            <device>4000</device>
        </enable_rules>
        <filters>
            <filter>.*InferRequestCheckTensorPrecision.*type=f8e4m3.*</filter>
            <filter>.*InferRequestCheckTensorPrecision.*type=f8e5m2.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=f8e4m3.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=f8e5m2.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Data types not supported by NPU3720 PV Driver.</message>
        <enable_rules>
            <device>3720</device>
            <driver_version>1688</driver_version>
        </enable_rules>
        <filters>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=boolean.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=i16.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=u16.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=bf16.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=i64.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=u64.*</filter>
            <filter>.*InferRequestIOTensorSetPrecisionTest.*type=f64.*</filter>
        </filters>
    </skip_config>

    <!-- C#128116 -->
    <skip_config>
        <message>Unicode paths for ov::cache_dir are not correctly handled on Windows</message>
        <enable_rules>
            <operating_system>windows</operating_system>
        </enable_rules>
        <filters>
            <filter>.*CompiledKernelsCacheTest.*CanCreateCacheDirAndDumpBinariesUnicodePath.*</filter>
        </filters>
    </skip_config>

    <!-- E#108600 -->
    <skip_config>
        <message>Unsupported NPU properties</message>
        <filters>
            <filter>.*OVCheckMetricsPropsTests_ModelDependceProps.*</filter>
        </filters>
    </skip_config>

    <!-- E#114623 -->
    <skip_config>
        <message>The private platform names cannot be identified via the \"ov::available_devices\" configuration.</message>
        <enable_rules>
            <device>!3720</device>
        </enable_rules>
        <filters>
            <filter>.*smoke_BehaviorTests_OVClassSpecificDeviceTest/OVSpecificDeviceGetConfigTest.GetConfigSpecificDeviceNoThrow.*</filter>
            <filter>.*smoke_BehaviorTests_OVClassSpecificDeviceTest/OVSpecificDeviceTestSetConfig.SetConfigSpecificDeviceNoThrow.*</filter>
        </filters>
    </skip_config>

    <!-- E#112064 -->
    <skip_config>
        <message>INFERENCE_PRECISION_HINT = FP32 not supported on NPU</message>
        <filters>
            <filter>.*CoreThreadingTestsWithIter.smoke_CompileModel_Accuracy_SingleCore.*</filter>
            <filter>.*CoreThreadingTestsWithIter.smoke_CompileModel_Accuracy_MultipleCores.*</filter>
        </filters>
    </skip_config>

    <!-- E#112064 -->
    <skip_config>
        <message>Failing Windows PV Driver core threading tests</message>
        <enable_rules>
            <device>3720</device>
            <driver_version>1688</driver_version>
            <operating_system>windows</operating_system>
        </enable_rules>
        <filters>
            <filter>.*CoreThreadingTest.smoke_QueryModel.*</filter>
        </filters>
    </skip_config>

    <!-- E#133153 -->
    <skip_config>
        <message>Failing properties tests</message>
        <filters>
            <filter>.*OVPropertiesIncorrectTests.SetPropertiesWithIncorrectKey.*DEVICE_ID.*</filter>
        </filters>
    </skip_config>

    <!-- E####### -->
    <skip_config>
        <message>Test will set NPU_TILES to -1 which is an invalid value, in this case default values don't work</message>
        <filters>
            <filter>.*OVCompiledModelPropertiesDefaultSupportedTests.CanCompileWithDefaultValueFromPlugin.*</filter>
        </filters>
    </skip_config>

    <!-- E#116575 -->
    <skip_config>
        <message>NPU fails for `OVIterationChaining.Simple` tests</message>
        <filters>
            <filter>.*OVIterationChaining.Simple.*</filter>
        </filters>
    </skip_config>

    <!-- E#116596 -->
    <skip_config>
        <message>Missing model ops in profiling info</message>
        <filters>
            <filter>.*OVInferRequestPerfCountersTest.CheckOperationInProfilingInfo.*</filter>
        </filters>
    </skip_config>

    <!-- E#118045 -->
    <skip_config>
        <message>A property with the name of the tensors must be set to support ROI Tensors</message>
        <filters>
            <filter>.*OVInferRequestInferenceTests.Inference_ROI_Tensor/roi_nchw.*</filter>
        </filters>
    </skip_config>

    <!-- E#121347 -->
    <skip_config>
        <message>Error message for empty model from stream must be changed to have \"device xml header\"</message>
        <filters>
            <filter>.*smoke_BehaviorTests/OVClassCompiledModelImportExportTestP.smoke_ImportNetworkThrowWithDeviceName.*</filter>
            <filter>.*smoke_Hetero_BehaviorTests/OVClassCompiledModelImportExportTestP.smoke_ImportNetworkThrowWithDeviceName.*</filter>
        </filters>
    </skip_config>

    <!-- E###### -->
    <skip_config>
        <message>NPU cannot set properties for compiled models except CACHE_ENCRYPTION_CALLBACKS, but is not convertible to string</message>
        <filters>
            <filter>.*OVClassCompiledModelSetCorrectConfigTest.canSetConfig.*</filter>
        </filters>
    </skip_config>

    <!-- C#139118 -->
    <skip_config>
        <message>Failing runtime model tests</message>
        <filters>
            <filter>.*OVCompiledModelGraphUniqueNodeNamesTest.CheckUniqueNodeNames.*</filter>
            <filter>.*OVExecGraphSerializationTest.ExecutionGraph.*</filter>
        </filters>
    </skip_config>

    <!-- E#162037 -->
    <skip_config>
        <message>compiled_blob test use `CACHE_MODE` which is not supported on NPU</message>
        <filters>
            <filter>.*OVCompiledModelBaseTest.compile_from_cached_weightless_blob_no_hint/.*</filter>
            <filter>.*OVCompiledModelBaseTest.compile_from_cached_weightless_blob_but_no_weights/.*</filter>
            <filter>.*OVCompiledModelBaseTest.compile_from_cached_weightless_blob_use_weight_hint/.*</filter>
            <filter>.*OVCompiledModelBaseTest.compile_from_regular_blob/.*</filter>
            <filter>.*OVCompiledModelBaseTest.compile_from_weightless_blob/.*</filter>
            <filter>.*OVCompiledModelBaseTest.compile_from_weightless_blob_but_no_weights/.*</filter>
            <filter>.*OVCompiledModelBaseTest.import_from_weightless_blob/.*</filter>
            <filter>.*OVCompiledModelBaseTest.use_blob_hint_has_priority_over_cache/.*</filter>
            <filter>.*OVCompiledModelBaseTest.use_blob_hint_has_priority_over_cache_but_weights_from_model_path/.*</filter>
            <filter>.*OVCompiledModelBaseTest.use_blob_hint_has_priority_over_cache_but_weights_bind_from_model_hint/.*</filter>
            <filter>.*OVCompiledModelBaseTest.use_blob_hint_which_fails_load_from_cache/.*</filter>
        </filters>
    </skip_config>

    <!-- C#191862 -->
    <skip_config>
        <message>Non "developer build" CiDs don't support weights separation yet</message>
        <filters>
            <filter>.*WeightsSeparationIterativeTests.*DRIVER.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Tests skipped to reduce overall log length</message>
        <filters>
            <filter>.*OVCheckSetSupportedRWMetricsPropsTests.*LOG_INFO.*</filter>
            <filter>.*OVCheckSetSupportedRWMetricsPropsTests.*LOG_DEBUG.*</filter>
            <filter>.*OVCheckSetSupportedRWMetricsPropsTests.*LOG_TRACE.*</filter>
        </filters>
    </skip_config>

    <!-- C#191861 -->
    <skip_config>
        <message>Blobs for OVBlobCompatibilityNPU need to be updated for NPU5010 and NPU6010</message>
        <enable_rules>
            <device>5010</device>
            <device>6010</device>
        </enable_rules>
        <filters>
            <filter>.*OVBlobCompatibilityNPU.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>PV driver cannot create infer request for models with boolean inputs</message>
        <enable_rules>
            <device>3720</device>
            <driver_version>1688</driver_version>
        </enable_rules>
        <filters>
            <filter>.*BooleanPrecisionInferRequestRunTests.*</filter>
        </filters>
    </skip_config>

    <!-- E#206393 -->
    <skip_config>
        <message>Skip memory standard allocation tests for Win NPU3720 due to OS issue, E#206393.</message>
        <enable_rules>
            <operating_system>windows</operating_system>
            <device>3720</device>
        </enable_rules>
        <filters>
            <filter>.*ZeroGraphTest.SetAlignedBlob.*</filter>
        </filters>
    </skip_config>

    <!-- C#187111 -->
    <skip_config>
        <message>TiWithLstmCell model won't provide `WeightlessCacheAttribute` in constants' rt_info</message>
        <filters>
            <filter>.*WeightlessCacheAccuracy.TiWithLstmCell.*ENABLE_WEIGHTLESS.*</filter>
        </filters>
    </skip_config>

    <!-- C#185847 -->
    <skip_config>
        <message>Write only (WO) properties will return empty ov::Any objects</message>
        <filters>
            <filter>.*OVClassCompiledModelPropertiesTests.canCompileModelWithPropertiesAndCheckGetProperty.*CACHE_ENCRYPTION_CALLBACKS.*</filter>
            <filter>.*OVPropertiesTests.canSetPropertyAndCheckGetProperty.*CACHE_ENCRYPTION_CALLBACKS.*</filter>
            <filter>.*OVPropertiesDefaultSupportedTests.CanSetDefaultValueBackToPlugin.*</filter>
            <filter>.*OVClassCompiledModelGetConfigTest.GetConfigNoThrow.*</filter>
            <filter>.*OVSpecificDeviceGetConfigTest.GetConfigSpecificDeviceNoThrow.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>UD12 driver does not support CACHE_ENCRYPTION_CALLBACKS on MTL</message>
        <enable_rules>
            <device>3720</device>
            <driver_version>2020568</driver_version>
        </enable_rules>
        <filters>
            <filter>.*OVClassCompiledModelPropertiesTests.CanUseCache.*CACHE_ENCRYPTION_CALLBACKS.*</filter>
            <filter>.*OVClassCompileModelWithCorrectPropertiesTest.*CACHE_ENCRYPTION_CALLBACKS.*</filter>
            <filter>.*OVClassCompiledModelGetPropertyTest.*CACHE_ENCRYPTION_CALLBACKS.*</filter>
            <filter>.*OVCompileModelGetExecutionDeviceTests.CanGetExecutionDeviceInfo.*CACHE_ENCRYPTION_CALLBACKS.*</filter>
            <filter>.*CompileModelWithCacheEncryptionTest.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>UD12 driver does not support secure compilation</message>
        <enable_rules>
            <driver_version>2020568</driver_version>
        </enable_rules>
        <filters>
            <filter>.*WeightlessCacheAccuracy.ReadConcatSplitAssign.*do_encryption=True.*(config=_|NPU_COMPILER_TYPE\[DRIVER\]).*</filter>
            <filter>.*WeightlessCacheAccuracy.SingleConcatWithConstant.*do_encryption=True.*(config=_|NPU_COMPILER_TYPE\[DRIVER\]).*</filter>
            <filter>.*WeightlessCacheAccuracy.TiWithLstmCell.*do_encryption=True.*config=_.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>PV driver cannot compile models securely</message>
        <enable_rules>
            <device>3720</device>
            <driver_version>1688</driver_version>
        </enable_rules>
        <filters>
            <filter>.*OVHoldersTestNPU.CompileModelWithEncryptionWorksAfterConfigDeallocate.*</filter>
        </filters>
    </skip_config>

    <skip_config>
        <message>Exclusive Async Request is not supported by NPU</message>
        <filters>
            <filter>.*InferRequestPropertiesTest.canSetExclusiveAsyncRequests.*</filter>
            <filter>.*InferRequestPropertiesTest.ReusableCPUStreamsExecutor.*</filter>
        </filters>
    </skip_config>

    <!-- E#216296 -->
    <skip_config>
        <message>Test failed with compile error.</message>
        <enable_rules>
            <device>3720</device>
        </enable_rules>
        <filters>
            <filter>.*SerializationTestNPUW.CompiledModelPhase0CompatibilityExportSucceedsWithStaticAttention.*</filter>
        </filters>
    </skip_config>

    <!-- E#218923 -->
    <skip_config>
        <message>The tests crash when compiling MaxPool model on NPU4000 and NPU5010</message>
        <enable_rules>
            <device>4000</device>
            <device>5010</device>
        </enable_rules>
        <filters>
            <filter>.*InferWithHostCompileTests.*model=MaxPool.*</filter>
        </filters>
    </skip_config>

</skip_configs>
